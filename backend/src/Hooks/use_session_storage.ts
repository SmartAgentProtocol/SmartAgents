const useSessionStorage: any = (key: string) => {
    // if (window !== undefined) {
    // const i = window.sessionStorage.getItem(key);
    return "yoo";
    // }
};

export default useSessionStorage;